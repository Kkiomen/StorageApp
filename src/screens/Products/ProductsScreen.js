import React from 'react'
import {StyleSheet, View, Text} from "react-native";

const ProductScreen = ({props,navigation}) => {
    return (
        <View>
            <Text></Text>
        </View>
    );
}

const styles = StyleSheet.create({});

export default ProductScreen;

